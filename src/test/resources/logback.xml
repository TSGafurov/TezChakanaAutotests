<?xml version="1.0" encoding="UTF-8"?>
<!--
     Диагностика прогона (preflight-чеклист, ретраи RetryOnce, сохранённые скриншоты
     падений) идёт через этот логгер вместо System.out.println - остаётся в
     target/logs/tests.log после прогона, не только в консоли Maven, которая часто
     теряется/обрезается при больших пачках тестов (см. Known issue 9 в
     docs/exploration-notes.md).
-->
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>target/logs/tests.log</file>
        <append>false</append>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>
</configuration>
